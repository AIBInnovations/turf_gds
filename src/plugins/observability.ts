import fp from'fastify-plugin';
const plugin=fp(async fastify=>{let requests=0,errors=0,totalMs=0;const started=new WeakMap<object,number>();fastify.addHook('onRequest',async r=>{started.set(r,performance.now());});fastify.addHook('onResponse',async(r,reply)=>{requests++;if(reply.statusCode>=500)errors++;totalMs+=Math.max(0,performance.now()-(started.get(r)??performance.now()));});fastify.get('/metrics',async(_r,reply)=>reply.type('text/plain; version=0.0.4').send(`# HELP gds_http_requests_total Total HTTP requests\n# TYPE gds_http_requests_total counter\ngds_http_requests_total ${requests}\n# HELP gds_http_errors_total Total HTTP 5xx responses\n# TYPE gds_http_errors_total counter\ngds_http_errors_total ${errors}\n# HELP gds_http_request_duration_ms_sum Sum of request duration in milliseconds\n# TYPE gds_http_request_duration_ms_sum counter\ngds_http_request_duration_ms_sum ${Math.round(totalMs)}\n`));});export default plugin;
