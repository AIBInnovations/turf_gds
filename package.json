{
  "name": "turf-gds-api-layer",
  "version": "0.1.0",
  "private": true,
  "description": "Fastify API layer for the Turf Booking GDS.",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/server.js",
    "worker:dev": "tsx watch src/communications-worker.ts",
    "worker:start": "node dist/communications-worker.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test test/**/*.test.ts",
    "test:load": "tsx test/load/partner-api-load.ts",
    "admin:create": "tsx src/scripts/create-admin.ts"
  },
  "dependencies": {
    "@fastify/multipart": "^10.1.0",
    "cloudinary": "^2.10.0",
    "dotenv": "^17.4.2",
    "fastify": "^5.10.0",
    "fastify-plugin": "^6.0.0",
    "fastify-raw-body": "^5.0.0",
    "mongodb": "^7.5.0",
    "redis": "^5.8.2"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "tsx": "^4.23.1",
    "typescript": "^7.0.2"
  }
}
